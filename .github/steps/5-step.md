## Step 5: Adicione um H1

Vamos criar o titulo principal da pagina.

### 📖 Teoria: A tag `h1`

A tag `<h1>` (de _heading 1_) define o título mais importante e principal da página. HTML possui uma hierarquia de títulos de `<h1>` a `<h6>`, sendo `<h1>` o mais importante e `<h6>` o menos.

**Boas práticas:**
- Use apenas **um** `<h1>` por página (o título principal)
- Mantenha a hierarquia: `<h2>` para subtítulos, `<h3>` para subseções, etc.
- Não pule níveis (ex: não vá de `<h1>` direto para `<h3>`)

**Importância:** Títulos ajudam na acessibilidade (leitores de tela usam para navegação) e no SEO (motores de busca consideram o `<h1>` muito importante).

📚 **Saiba mais:** [Elementos de cabeçalho - MDN](https://developer.mozilla.org/pt-BR/docs/Web/HTML/Element/Heading_Elements)

### ⌨️ Atividade: Titulo principal

1. Dentro do `body`, adicione uma tag `h1` com o texto:

   ```
   Seja bem-vindo ao meu site!
   ```

1. Exemplo:

   ```html
   <h1>Seja bem-vindo ao meu site!</h1>
   ```

1. Salve, faca commit e envie para o GitHub.

<details>
<summary>Having trouble? 🤷</summary><br/>

- O `h1` deve ficar dentro do `body`.
- O texto pode ter pequenas variacoes, mas mantenha a frase principal.

</details>
